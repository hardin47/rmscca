build.B <- function(k,p,q){
  B <- matrix(rep(0,p*q),nrow = p, ncol = q)
  
  for(i in 1:12){
    for(j in 1:8){
      B[i,j] <- k
    }
  }
  for(i in 13:20){
    for(j in 9:12)
      B[i,j] <- k
  }
  for(i in 21:32){
    for(j in 13:16)
      B[i,j] <- k
  }
  
  
  return(B)
}
